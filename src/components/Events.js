import{ useState, useEffect} from 'react'
import $ from 'jquery'

const Events = () => {
  return (
    <div id='eventsHome' style={{height: '100vh', width: '100%', backgroundColor: '#2b1914'}}>
      <button id='eventBtn' style={{width: '100px', height: '100px', borderRadius: '100%'}}></button>
    </div>
  )
}

export default Events